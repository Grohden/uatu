{
  "name": "uatu",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "fmt": "dprint fmt",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "yarn compile && yarn cp:assets",
    "compile": "npx spack",
    "serve": "npx http-serve ./dist",
    "cp:assets": "cp -a ./src/assets/. ./dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@swc/cli": "^0.1.55",
    "@swc/core": "^1.2.151",
    "chokidar": "^3.5.3",
    "dprint": "^0.31.1",
    "typescript": "^4.6.2"
  }
}
